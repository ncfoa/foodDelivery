import { StyleSheet, Text, View } from "react-native";
import React from "react";

const BetweenSectionSeparator = ({ height }: any) => {
  return <View style={{ height: height, backgroundColor: "#111826" }} />;
};

export default BetweenSectionSeparator;
